# ============================================
# 🔒 VARIABLES ENVIRONNEMENT SERVEUR - TEMPLATE
# ============================================
#
# ⚠️ INSTRUCTIONS :
# 1. Copier ce fichier vers .env.server
# 2. Remplacer TOUS les placeholders YOUR_*
# 3. Générer secrets sécurisés (voir commandes ci-dessous)
# 4. NE JAMAIS committer .env.server (.gitignore)
#
# ============================================

# 🤖 Gemini API Key (Google AI Studio)
# Obtenir la clé : https://aistudio.google.com/app/apikey
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE

# 🔐 JWT Secret (pour authentification)
# ⚠️ GÉNÉRER UN SECRET UNIQUE :
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=YOUR_JWT_SECRET_MINIMUM_64_CHARS_GENERATE_WITH_CRYPTO

# 🌍 Configuration serveur
PORT=3001
HOST=127.0.0.1
NODE_ENV=development

# 🔗 Frontend URL (CORS)
FRONTEND_URL=http://localhost:5173

# 📊 Database PostgreSQL (Docker)
# Production : ajouter ?sslmode=require
DATABASE_URL=postgresql://transit_user:YOUR_DB_PASSWORD@localhost:5433/transit_guinee?schema=public

# 🔴 Redis (rate limiting)
# Production : ajouter password avec --requirepass
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=YOUR_REDIS_PASSWORD_OPTIONAL

# 🔐 2FA Configuration
TWO_FACTOR_ISSUER=TransitGuinée

# 📝 Logging (debug | info | warn | error)
LOG_LEVEL=debug

# ============================================
# GÉNÉRATION SECRETS PRODUCTION
# ============================================
#
# JWT_SECRET (64 bytes) :
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#
# REDIS_PASSWORD (32 bytes) :
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# DATABASE_PASSWORD (24 chars alphanumeric) :
# node -e "console.log(require('crypto').randomBytes(18).toString('base64').replace(/[^a-zA-Z0-9]/g, '').substring(0, 24))"
#
# ============================================

# ============================================
# DÉPLOIEMENT PRODUCTION
# ============================================
#
# NETLIFY :
# Site Settings > Environment Variables > Add variable
# Ajouter toutes les variables ci-dessus
#
# VERCEL :
# Project Settings > Environment Variables > Add New
# Ajouter toutes les variables ci-dessus
#
# VPS (Linux) :
# Créer /opt/transit/.env.server avec ces variables
# sudo chown transit:transit /opt/transit/.env.server
# sudo chmod 600 /opt/transit/.env.server
#
# DOCKER :
# Utiliser docker-compose.yml env_file: .env.server
# OU passer via -e flag pour chaque variable
#
# ============================================

# ============================================
# SÉCURITÉ PRODUCTION - CHECKLIST
# ============================================
#
# [ ] JWT_SECRET différent dev/staging/prod
# [ ] GEMINI_API_KEY rate limits configurés Google Cloud
# [ ] DATABASE_URL avec SSL (?sslmode=require)
# [ ] REDIS_PASSWORD activé (requirepass redis.conf)
# [ ] NODE_ENV=production (vérifie VITE_USE_MOCK=false)
# [ ] LOG_LEVEL=warn (réduit verbosité logs)
# [ ] Rotation JWT_SECRET tous les 90 jours
# [ ] Backup secrets dans vault équipe (1Password/Bitwarden)
#
# ============================================
