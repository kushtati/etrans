# ============================================
# 🎨 FRONTEND (Vite - variables VITE_*)
# ============================================
# Mode développement avec API réelle

NODE_ENV=development

# Mock data toggle (set true pour données fictives offline)
VITE_USE_MOCK=false

# Enable verbose logging console
VITE_DEBUG=true

# API endpoint (proxy vers backend via vite.config.ts)
VITE_API_URL=/api

# Max logs in-memory frontend
VITE_LOG_BUFFER_SIZE=1000

# ============================================
# 🖥️ BACKEND (Docker/Local server)
# ============================================

# Backend Express port (server only - not exposed to frontend)
PORT=3001

# 🔐 JWT Secret (backend auth - DEV ONLY)
# ⚠️ Ce secret DEV doit DIFFÉRER de production
# ⚠️ Tokens signés avec cette clé ne sont valides QUE en développement
# Production: utiliser .env.server avec crypto.randomBytes(64)
JWT_SECRET=dev-only-secret-NEVER-use-in-production-must-be-64chars-minimum

# 📊 Database PostgreSQL (DEV ONLY)
# ⚠️ Credentials par défaut postgres:postgres
# ⚠️ NE JAMAIS utiliser ces credentials en staging/production
# Production: utilisateur dédié avec password fort dans .env.server
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/transit_dev

# ============================================
# 🔐 AUTRES CONFIGURATIONS
# ============================================

# 2FA Google Authenticator label
TWO_FACTOR_ISSUER=TransitGuinée-Dev

# Logging level (debug | info | warn | error)
LOG_LEVEL=debug
