# ============================================
# 🚂 NIXPACKS CONFIG - Railway Backend Build
# ============================================
# 
# Build optimisé pour backend Node.js + Prisma
# Frontend déployé séparément sur Vercel
#
# ============================================

[phases.setup]
nixPkgs = ["nodejs_20", "openssl", "postgresql"]

[phases.install]
cmds = [
  "npm ci --omit=dev",
  "npm install -g tsx prisma"
]

[phases.build]
cmds = [
  "npx prisma generate",
  "npm run build:server"
]

[start]
cmd = "npm run start:railway"

[variables]
NODE_ENV = "production"
