# ============================================
# 🚀 PRODUCTION ENVIRONMENT - Railway/Vercel
# ============================================
# ⚠️ CE FICHIER EST UN TEMPLATE - NE PAS COMMITTER
# Les valeurs doivent être configurées dans Railway Dashboard
# ============================================

# 🌍 Environment
NODE_ENV=production

# 🔐 JWT Secret - GÉNÉRER AVEC:
# PowerShell: [Convert]::ToBase64String((1..64|%{Get-Random -Max 256}))
# Linux: openssl rand -base64 64
JWT_SECRET=MUST_BE_SET_IN_RAILWAY_DASHBOARD

# 🖥️ Server Configuration
PORT=3001
HOST=0.0.0.0

# 🔗 Frontend URL (votre URL Vercel)
FRONTEND_URL=https://votre-app.vercel.app

# 📊 Database - RÉFÉRENCE Railway PostgreSQL
# Dans Railway: Add Reference → PostgreSQL → DATABASE_URL
DATABASE_URL=REFERENCE_RAILWAY_POSTGRES

# 🔴 Redis - RÉFÉRENCE Railway Redis
# Dans Railway: Add Reference → Redis → REDIS_URL
REDIS_URL=REFERENCE_RAILWAY_REDIS

# 🤖 Gemini API (optionnel)
GEMINI_API_KEY=OPTIONNEL_VOTRE_CLE

# 🔐 2FA Configuration
TWO_FACTOR_ISSUER=TransitGuinée

# 📝 Logging (info ou warn en production)
LOG_LEVEL=info

# ============================================
# 🎯 INSTRUCTIONS RAILWAY
# ============================================
#
# 1. Ajouter PostgreSQL Database:
#    New → Database → PostgreSQL
#    → Cela créera automatiquement DATABASE_URL
#
# 2. Ajouter Redis Database:
#    New → Database → Redis
#    → Cela créera automatiquement REDIS_URL
#
# 3. Dans votre service Backend → Variables:
#    - Cliquez "New Variable" → "Add Reference"
#    - Sélectionnez PostgreSQL → DATABASE_URL
#    - Sélectionnez Redis → REDIS_URL
#
# 4. Ajouter les autres variables manuellement:
#    NODE_ENV=production
#    JWT_SECRET=[générez avec commande ci-dessus]
#    PORT=3001
#    HOST=0.0.0.0
#    FRONTEND_URL=[votre URL Vercel]
#    LOG_LEVEL=info
#
# 5. Railway redéploiera automatiquement
#
# ============================================

# ============================================
# ⚠️ SÉCURITÉ CRITIQUE
# ============================================
#
# ❌ NE JAMAIS committer ce fichier avec vraies valeurs
# ❌ NE JAMAIS utiliser les mêmes secrets dev/prod
# ✅ Générer JWT_SECRET unique pour prod (min 64 chars)
# ✅ Activer SSL pour PostgreSQL en production
# ✅ Sauvegarder secrets dans password manager
# ✅ Rotation JWT_SECRET tous les 90 jours
#
# ============================================
